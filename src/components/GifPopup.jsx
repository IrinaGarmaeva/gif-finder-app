import React from "react";
/*
function GifPopup({ gif, onClose }) {
  return (
    <section
      className={`popup popup_type_image-zoom ${
        gif.images.fixed_width.url ? `popup_opened` : ""
      }`}
    >
      <div className="popup__image-container">
        <button className="popup__close-button" onClick={onClose}></button>
        <img className="popup__image" src={gif.images.fixed_width.url} alt={gif.title} />
        <p className="popup__description">{gif.title}</p>
      </div>
    </section>
  );
}

export default GifPopup;*/
